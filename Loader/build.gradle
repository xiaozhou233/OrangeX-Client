plugins {
    id 'java'
    id "com.gradleup.shadow" version "8.3.9"
}

group = 'cn.xiaozhou.orangex.loader'
version = 'unspecified'

repositories {
    mavenCentral()
}

dependencies {
    compileOnly(files("../libraries/JuiceAgent-API-1.4.0+build.1.jar"))
}
